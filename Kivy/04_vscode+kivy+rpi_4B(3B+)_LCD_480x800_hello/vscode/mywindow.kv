# import kivy.utils
#:import hex kivy.utils.get_color_from_hex

<MyToggleButton>:
    # rows:2
    spacing: 50
    # paddig: 120
    # Padding between layout box and children: [padding_left, padding_top, padding_right, padding_bottom].
    # padding also accepts a two argument form [padding_horizontal, padding_vertical] and a one argument form [padding].
    padding_vertical: 100
    padding_bottom: 50
    orientation: 'vertical'
    canvas.before:
        # Color:
        #     rgba: 1, 0, 1, 0.5
        # Rectangle:
        # BorderImage:
        BorderImage:
            pos: self.pos
            size: self.size
        #     source: "nature.png"
            source: "background.png"

    # canvas.color: 1, 1, 0
    # canvas.background_color: 1, 1, 0
    RelativeLayout:
        id: rl
        size_hint_x: 0.9
        size_hint_y: None
        pos_hint_x: None
        pocs_hint_y: None
        # x, y, width, height
        y: 300
        x: 100
        height: 300
        # size_y: 400
        # height: 500
        # pos_hint: {'center_x': .5 } # , 'center_y': None'center_y': .5
        # pos_y: 100
        # spacing: 10
        # paddig: 20
        canvas.before:
            Color:
                rgba: 1, 0, 0, 0.5
            Rectangle:                
                size: self.size
                # size: (self.size[0]/2, self.size[1]/2)
        Label:
            text:'Hello World'
            font_size:72
            background_color: 'red'
            id: lb

    RelativeLayout:
        size_hint_x: 0.9
        size_hint_y: None
        # pos_hint: None, None
        # size_y: 400
        # height: 500
        # pos_hint: {'center_x': .5, 'center_y': .5}
        # spacing: 10
        # paddig: 20
        canvas.before:
            Color:
                rgba: 1, 0, 0, 0.5
            Rectangle:                
                size: self.size
                # size: (self.size[0]/2, self.size[1]/2)
        Label:
            text:'Hello '
            font_size:72
            background_color: 'red' # hex('#112233') # get_color_from_hex('#eaf903') # (1, 1, 1, 1)
            # paddig: 20

    # BoxLayout:
    #     ToggleButton:
    #         # size_hint:None,None
    #         # size:0.25 * root.width, 0.25 * root.height
    #         # pos:0.125 * root.width, 0.350 * root.height
    #         text:"Toggle One"

    #     ToggleButton:
    #         size_hint:None,None
    #         size:128, 128
    #         # size:0.25 * root.width, 0.25 * root.height
    #         # pos:0.125 * root.width, 0.350 * root.height
    #         text:"Toggle Two"
    #         # https://icon-icons.com/
    #         background_normal:'viber.png'
    #         background_down: 'whatsapp.png'
